# Server Guard - Unified Requirements
# All dependencies for the consolidated single-container deployment

# Use CPU-only PyTorch for smaller package size on Render free tier
--extra-index-url https://download.pytorch.org/whl/cpu

# FastAPI and ASGI server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# Socket.IO for real-time communication
python-socketio>=5.10.0

# HTTP client for inter-service communication
aiohttp>=3.9.0

# Environment and file handling
python-dotenv>=1.0.0
aiofiles>=23.2.1
python-multipart>=0.0.9

# Flask for Model Service
flask==3.0.0
flask-cors==4.0.0
gunicorn>=21.0.0

# ML/AI dependencies (using CPU-only torch for smaller size)
torch>=2.0.0
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
joblib>=1.3.0
scikit-learn>=1.3.0
requests>=2.31.0
